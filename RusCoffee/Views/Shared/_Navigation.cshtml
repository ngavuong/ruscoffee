@using RusCoffee.BL;
@using Microsoft.AspNetCore.Http;

@{
    var token = Context.Session.GetString("JWToken");
    var user = CommonFn.GetUserInfo(token);
}

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    @*<img alt="image" class="rounded-circle" src="~/images/hot_coffee.png" style="width:100%"/>*@
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold" id="fullName">@user.FullName</span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        <li>@*<a class="dropdown-item" href="@Url.Action("Profile", "AppViews")">Profile</a>*@</li>
                        <li><a class="dropdown-item" href="@Url.Action("ChangePassword", "AppViews")"> Đổi mật khẩu</a></li>
                        <li class="dropdown-divider"></li>
                        <li><a href="@Url.Action("Logout", "Home")" class="dropdown-item logOutBtn">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    IN+
                </div>
            </li>
            @if (user.RoleType == 1)
            {
                <li class="@Html.IsSelected(controller: "Dashboards")">
                    <a href="@Url.Action("Dashboard", "Dashboards")"><i class="fa fa-area-chart"></i> <span class="nav-label">Thống kê doanh số</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("ItemList", "Tables")"><i class="fa fa-bars"></i> <span class="nav-label">Quản lý món</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("EmployeeList", "Tables")"><i class="fa fa-address-book"></i> <span class="nav-label">Danh sách nhân viên</span> </a>
                </li>

                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("OrderMenu", "AppViews")"><i class="fa fa-shopping-cart"></i> <span class="nav-label">Đặt món</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("InvoiceList", "Tables")"><i class="fa fa-pie-chart"></i> <span class="nav-label">Danh sách hóa đơn</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("MaterialList", "Tables")"><i class="fa fa-sign-in"></i> <span class="nav-label">Nhập kho</span> </a>
                </li>
                @*<li class="special_link @Html.IsSelected(controller: " Landing")">
                        <a href="@Url.Action("Index", "Landing")"><i class="fa fa-star"></i> <span class="nav-label" >Landing page</span></a>
                    </li>*@
            }
            else
            {
                <li class="@Html.IsSelected(controller: "AppViews")">
                    <a href="@Url.Action("OrderMenu", "AppViews")"><i class="fa fa-shopping-cart"></i> <span class="nav-label" data-i18n="nav.metrics">Đặt món</span> </a>
                </li>
                <li class="@Html.IsSelected(controller: "Tables")">
                    <a href="@Url.Action("MaterialList", "Tables")"><i class="fa fa-sign-in"></i> <span class="nav-label" data-i18n="nav.metrics">Nhập kho</span> </a>
                </li>
            }
        </ul>
    </div>
</nav>
<script src="~/Scripts/libraries/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/base/ajax.js"></script>
<script src="~/Scripts/views/Navigation.js"></script>